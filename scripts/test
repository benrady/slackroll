#!/usr/bin/env bash

abspath="$(cd "${0%/*}" 2>/dev/null; echo "$PWD"/"${0##*/}")"
bin_dir=`dirname "$abspath"`

cd $bin_dir/..
echo $PWD

if [[ ! -e .testdeps/pytest.py ]]; then
  echo "Installing pytest"
  make .testdeps
fi

python .testdeps/pytest.py --color=yes test
